package com.example.mini_chat_test.ui.theme

import androidx.compose.ui.graphics.Color

val PrimaryBlue = Color(0xFF006D77)


val Secondary = Color(0xFF83C5BE)


val AccentTeal = Color(0xFF00C897)


val MessageBubbleReceiver = Color(0xFFE5E5EA)


val BackgroundWhite =Color(0xFFF0F4F8)
val TextDark = Color(0xFF1C1C1E)

val surface =  Color(0xFFFFFFFF)

val SenderBubbleColor = PrimaryBlue

val ReceiverBubbleColor = MessageBubbleReceiver


// Dark Theme Background & Text Colors
val DarkBackground = Color(0xFF121212) // Very dark gray, standard for dark mode
val DarkSurface = Color(0xFF1E1E1E)   // Slightly lighter gray for cards/surfaces
val LightText = Color(0xFFFFFFFF)    // White text for contrast
val SubtleText = Color(0xFFAFAFAF)   // Lighter gray for secondary text/timestamps

// Accent Colors (Same as light theme for consistency)
val DarkPrimaryBlue = Color(0xFF0A84FF) // A slightly brighter blue works better on dark backgrounds
val DarkAccentTeal = Color(0xFF30D5C8)  // A vibrant teal accent
val DarkSecondary = Color(0xFF94D2BD)